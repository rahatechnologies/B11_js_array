const originalTestResults = [1, 5.3, 1.5, 10.99, -5, 10];

// originalTestResults.push(6, 7, 9.45, -3);

const storedResults = originalTestResults.concat([3.99, 2]);

originalTestResults.push(5.92);

console.log(storedResults, originalTestResults);
